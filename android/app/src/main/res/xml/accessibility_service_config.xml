<?xml version="1.0" encoding="utf-8"?>
<!--
    Accessibility service config for AIMxASSIST auto-shoot.
    The key flags:
      canPerformGestures="true"  — allows dispatchGesture() to inject swipes
    We do NOT restrict packageNames so the service can shoot regardless of
    which activity Carrom Disc Pool is currently showing.
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canPerformGestures="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100"
    android:settingsActivity="com.bitaim.carromaim.MainActivity" />
